version: "3.8"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: plane-discord-bot
    restart: unless-stopped
    env_file:
      - .env
    # If the bot logs output to the console, you can see it with docker compose logs
